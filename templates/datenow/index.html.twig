{% extends 'base.html.twig' %}

{% block title %}Datenow index{% endblock %}

{% block body %}
    <h1>Datenow index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datenow</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for datenow in datenows %}
            <tr>
                <td>{{ datenow.id }}</td>
                <td>{{ datenow.datenow ? datenow.datenow|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_datenow_show', {'id': datenow.id}) }}">show</a>
                    <a href="{{ path('app_datenow_edit', {'id': datenow.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_datenow_new') }}">Create new</a>
{% endblock %}
