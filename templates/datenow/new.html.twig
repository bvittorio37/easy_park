
{% extends 'adminTemplate.html.twig' %}

{% form_theme form 'form_div_layout.html.twig' %}

{% block pageName %} 
Place
{% endblock %}

{% block body %}
<section class="content">
      <div class="container-fluid">
        <div class="row">
            <!-- left column -->
            <div class="col-md-12">
                <div class="card card-primary">
                <div class="card-header">
                    <h1 class="card-title">Ajouter une nouvelle Place</h1>
                </div>
                <!-- /.card-header -->
                <!-- form start -->
                {{ form_start(form) }}
                    <div class="card-body">
                    <div class="form-group">
                        {{ form_label(form.datenow) }}
                        {{form_widget(form.datenow, { 'attr': {'class': 'form-control'} }) }}

                    </div>
                   
                    <!-- /.card-body -->
                    <div class="card-footer">
                    <button type="submit" class="btn btn-primary">Ajouter</button>
                    </div>
                {{ form_end(form) }}
                </div>
            </div>
            <!-- /.card -->
        </div>
</section>
{% endblock %}