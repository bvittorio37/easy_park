{% extends 'base.html.twig' %}

{% block title %}Datenow{% endblock %}

{% block body %}
    <h1>Datenow</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ datenow.id }}</td>
            </tr>
            <tr>
                <th>Datenow</th>
                <td>{{ datenow.datenow ? datenow.datenow|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_datenow_index') }}">back to list</a>

    <a href="{{ path('app_datenow_edit', {'id': datenow.id}) }}">edit</a>

    {{ include('datenow/_delete_form.html.twig') }}
{% endblock %}
