{% extends 'base.html.twig' %}

{% block title %}Parking{% endblock %}

{%  block body %}
    <h1>Parking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parking.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ parking.matricule }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ parking.debut ? parking.debut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Sortie</th>
                <td>{{ parking.sortie ? parking.sortie|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parking_index') }}">back to list</a>

    <a href="{{ path('app_parking_edit', {'id': parking.id}) }}">edit</a>

    {{ include('parking/_delete_form.html.twig') }}
{% endblock %}
