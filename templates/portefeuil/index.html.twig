{% extends 'base.html.twig' %}

{% block title %}Portefeuil index{% endblock %}

{% block body %}
    <h1>Portefeuil index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Solde</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for portefeuil in portefeuils %}
            <tr>
                <td>{{ portefeuil.id }}</td>
                <td>{{ portefeuil.solde }}</td>
                <td>
                    <a href="{{ path('app_portefeuil_show', {'id': portefeuil.id}) }}">show</a>
                    <a href="{{ path('app_portefeuil_edit', {'id': portefeuil.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_portefeuil_new') }}">Create new</a>
{% endblock %}
