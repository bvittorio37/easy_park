{% extends 'base.html.twig' %}

{% block title %}Tarif{% endblock %}

{% block body %}
    <h1>Tarif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarif.id }}</td>
            </tr>
            <tr>
                <th>Temps</th>
                <td>{{ tarif.temps ? tarif.temps|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ tarif.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tarif_index') }}">back to list</a>

    <a href="{{ path('app_tarif_edit', {'id': tarif.id}) }}">edit</a>

    {{ include('tarif/_delete_form.html.twig') }}
{% endblock %}
